# YAHYA Sales Dashboard — заметки для Claude

## Что это
Streamlit-дашборд по продажам сети YAHYA (шоколад, кофейня, кухня).
Хостится на Streamlit Cloud, данные раздаются через GitHub Pages.

Репозиторий: `MonkeyDjack/yahya-sales-dashboard` (приватный).
Главный файл приложения (entrypoint Streamlit Cloud): `dashboard_v2/app2.py`.

## Архитектура дашборда (редизайн 2026-06)

Мультистраничное приложение на `st.navigation` (страницы в sidebar, секциями).
Считается только открытая страница. Графики — Plotly.

```
dashboard_v2/
  app2.py            # entrypoint: set_page_config → load → навигация → sidebar → pg.run()
  core/
    config.py        # URL'ы GH Pages, COLORS, A_THR/B_THR, OWN_PERIOD_PAGES
    data.py          # load_sales/load_cost_reference/load_bom (только GH Pages,
                     #   fallback на локальные docs/ для офлайн-разработки)
    events.py        # BRANCH_EVENTS + lfl_split — like-for-like логика (см. ниже)
    periods.py       # пресеты периода, prev/yoy(−364), compare_kpis (Δ% по /день)
    filters.py       # sidebar: пресеты + каскадная форма фильтров
    helpers.py       # money/num, KPI, ABC, прогноз, cross-sell, safety stock, BOM
    excel.py         # df_to_sheet/dl_btn + plain_* (PatternFill — позиционная форма!)
    charts.py        # Plotly-тема: line_ts, compare_ts, pareto, heatmap, barh_top
    ui.py            # CSS, insight_card, freshness_badge, own_period_note
    context.py       # AppContext (df, df_f, df_universe, cost_ref, периоды)
  views/             # страницы (НЕ переименовывать в pages/ — конфликт legacy-MPA)
    overview.py      # Обзор: KPI, LFL-сравнения /день, график, инсайты, филиалы
    dynamics.py      # Тренд+сравнение (глоб. период) + Выгрузка (свой период)
    abc.py           # ABC/Pareto — перенесена из старой вкладки 3, ключи abc_*
    products.py      # Карточка товара + Календарь SKU (matplotlib) + Сезонность
    basket.py        # Cross-sell + Прогноз
    branches.py      # Рейтинг точек + Пики времени
    plan_fact.py     # План/Факт (свой период)
    stock.py         # Склад + наборы
  smoke_test.py      # AppTest-прогон всех 8 страниц (python dashboard_v2/smoke_test.py)
```

### Принципы честных цифр (важно не сломать!)
- **Период по умолчанию — последние 30 дней ДАННЫХ** (не вся история, не календарь).
  Пресеты в sidebar отсчитываются от `max_d` данных.
- **LFL (like-for-like)**: при сравнении двух периодов исключаются филиалы,
  не работавшие ≥95% дней в обоих окнах. События — в `core/events.py: BRANCH_EVENTS`.
  **При открытии нового филиала или закрытии/реконструкции — добавить запись туда!**
- **Δ% считаются по среднему/день** — неравные периоды сопоставимы (`compare_kpis`).
- **YoY = сдвиг −364 дня** (52 недели, сохраняет день недели), не −365.
- Бейдж свежести данных в шапке (красный, если данным >7 дней).
- Сезонность считается по всей истории — на странице стоит предупреждение.

### Состояние виджетов между страницами
Streamlit чистит state виджетов, не отрисованных в текущем ране. В `app2.py`
есть pin-блок (`_PIN_PREFIXES`/`_PIN_KEYS`), который сохраняет периоды ABC,
выгрузки и счётчик план/факта при переключении страниц. Новые «свои периоды»
делать по паттерну `_period_picker` (init по key, без `value=`) и добавлять в pin.

## Источники данных

| Файл | Что | Где раздаётся |
|---|---|---|
| `docs/база.parquet` | основная база продаж (~370 тыс строк, период 2025-01-01 → ...) | GitHub Pages |
| `docs/list1.parquet` | справочник SKU (Группа/Категория/Подкатегория) | GitHub Pages |
| `docs/себестоимость.parquet` | справочник себеса (Номенклатура → Себес полн., Розница) | GitHub Pages |
| `docs/разбивка_наборов.xlsx` | BOM для шоколадных наборов | GitHub Pages |
| `docs/Итоговый_отчет1.xlsx` | устаревший: исходный xlsx из 1С (parquet уже актуальнее) | GitHub Pages |

URL шаблон: `https://monkeydjack.github.io/yahya-sales-dashboard/<filename>`.

Дашборд кешит загрузку через `@st.cache_data(ttl=3600)` — изменения в parquet проявляются до 1 часа.

## Структура продаж (parquet)

Колонки: `Дата` (Timestamp), `Время` (str), `Филиал`, `Точки`, `Номенклатура`,
`Склад/Товар` (= чек), `Количество`, `Цена`, `Сумма`, `Месяц`, `Год`,
`Группа`, `Категория`, `Подкатегория`.

### Филиалы (на 2026-05)
- Старые (с 2025): ЦУМ, АЗИЯ МОЛЛ, ЭРКИНДИК, Бишкек Парк, АЭРОПОРТ
- Новые (открыты в 2026): NGROUP, АЗБУКА
- **АЗИЯ МОЛЛ** был на реконструкции **08.02.2026 – 01.03.2026** (22 дня) —
  это объясняет провал по февралю 2026 и **не** является сигналом падения спроса.

### Известные особенности данных
- SKU с суффиксом «**В**» в конце = «Вынос». Себес и розница такие же,
  как у базовой версии без «В». `build_cost_reference.py` обрабатывает это
  через расширенный `variants()`.
- В прайсах себеса часто бывают **«1 шт»** и **«(1конфета)»** в скобках,
  скобки нормализуются при матчинге.
- Покрытие себесом по выручке: **~84.5%** (Шоколад/Кухня/Бар).
  Без себеса остаются: сезонные (нет файла), шококарандаши (нет в прайсе),
  снятые с производства SKU.

## Скрипты в `dashboard_v2/`

| Скрипт | Что делает | Когда запускать |
|---|---|---|
| `app2.py` | Streamlit-приложение (запускать Streamlit'ом) | в проде |
| `category_mapping.py` | Канонический мэппинг Категория→Группа | импортируется |
| `fix_categories.py` | Однократный фикс категорий в parquet (мода на SKU, fallback Подкатегория ← Категория, ручные оверрайды для Нац.коллекций) | при импорте новых данных или после ручной правки в 1С |
| `build_cost_reference.py` | Собирает `docs/себестоимость.parquet` из xlsx-прайсов в `D:/claude/` | при обновлении прайса |
| `abc_chocolate_report.py` | Standalone-отчёт по шоколаду (Excel, 7 листов) | по запросу |
| `build_chocolate_director_report.py` | Word-документ для директора по шоколаду | по запросу |
| `append_*.py` | Разовые добавления новых месяцев в `база.parquet` | при ежемесячном обновлении |

### Бэкапы parquet
Перед массовыми правками `база.parquet` создаются `.bak_*`-файлы:
- `база.parquet.bak_before_category_fix` — до фикса категорий
- `база.parquet.bak_before_may2026`, `_before_fact16`, `_before_fact17` — до append'ов
Их **не коммитят** в репо (большие, не нужны).

## ABC-страница
Живёт в `dashboard_v2/views/abc.py` (перенесена из старой вкладки 3 почти
дословно, ключи виджетов `abc_*` сохранены). Поддерживает:
- **Режим «Один период»** + **Режим «Сравнение двух периодов»**
- Свой выбор периода (не глобальный date-фильтр)
- Пресеты: YTD vs прошлый год · Последние 30 vs пред. 30 · Месяц vs прошлый год · Квартал vs прошлый год
- Авто-обрезка длинного периода до короткого
- 4 уровня агрегации (Номенклатура / Подкатегория / Категория / Группа)
- 3 метрики (Сумма / Количество / Маржа)
- 6 под-вкладок при сравнении: Pareto-Drift · ABC-матрица переходов ·
  Growers · Decliners · Новинки/Снятые · Кандидаты на вывод

В коде использует `ctx.df_universe` (df с глобальными фильтрами, но БЕЗ date-фильтра)
и `ctx.cost_ref` (справочник себеса) из `core/context.py`.

## Excel-форматирование
**Важно**: openpyxl по умолчанию НЕ пишет `applyFill="1"` в `<xf>`,
из-за чего старые версии Excel и LibreOffice **игнорируют заливку**
(но показывают цвет шрифта). Решение — использовать **xlsxwriter**
(`abc_chocolate_report.py`).

Если будешь писать Excel через openpyxl и нужны заливки — используй
форму `PatternFill(start_color="...", end_color="...", fill_type="solid")`,
не `PatternFill("solid", fgColor="...")`. В `core/excel.py` уже исправлено.

## Категории — частые проблемы
1. **Подкатегория = «Без подкатегории»** — после `fix_categories.py`
   автоматически заменяется на название Категории.
2. **SKU = «Конфеты / Корпусные конфеты»** как fallback — багованный импорт
   из 1С ставит это часть строк у произвольных SKU. `fix_categories.py`
   убирает через выбор канона по моде.
3. **Нац.коллекция × 6** — 5 из них канонично «Наборы и коробки / Наборы /
   Нац. конфеты», 1 («соленая карамель и талкан») в исходных данных была
   «Плитки / Нац. плитки» — есть ручной оверрайд в `fix_categories.py`.
4. **Шоколадные наборы из «Наборы и коробки»** (Нац.коллекция, Корпусная/Нарезная/
   Коктельная коллекция, Набор из дубайских мини плиток, Слива-Грецкий НК, Ынтымак)
   биз-логически — шоколад. В отчётах по шоколаду включаются явным списком префиксов.

## Тон общения с пользователем
- Пользователь — `Muhitdin Yahya` (`muhitdin0@gmail.com`)
- Работает на русском, в смешанном стиле (терминология бизнеса + код)
- Любит конкретику: цифры, проценты, диффы. Не любит пустой воды.
- Когда задаёт вопрос («что это», «как сделать») — короткий ответ. Когда даёт задание — выполнить.
- Перед потенциально разрушительными действиями (push, удаление, переписывание данных) — спросить подтверждения.

## Полезные команды

```powershell
# Запуск дашборда локально (venv: D:\dashboard\.venv)
streamlit run dashboard_v2/app2.py

# Smoke-тест: рендер всех 8 страниц без браузера (после правок в core/views)
python dashboard_v2/smoke_test.py

# Пересобрать справочник себеса (когда обновили xlsx в D:/claude/)
python dashboard_v2/build_cost_reference.py

# Применить фикс категорий после нового импорта
python dashboard_v2/fix_categories.py

# Сгенерить шоколадный ABC-отчёт
python dashboard_v2/abc_chocolate_report.py
python dashboard_v2/build_chocolate_director_report.py
```

## История важных изменений
- `89f308d` — ABC-отчёт + однократный фикс категорий (скрипты)
- `0227069` — фикс категорий в parquet (применено к данным)
- `ddad196` — справочник себеса `docs/себестоимость.parquet`
